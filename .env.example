# Copy to .env and fill in. Never commit .env.

# Anthropic API key — Claude API direct (not Bedrock).
ANTHROPIC_API_KEY=YOUR_API_KEY

# Local development only; on Lambda these come from the SAM stack.
UPLOADS_BUCKET=
REVIEWS_TABLE=
REVIEW_STATUS_TABLE=
CORS_ALLOW_ORIGINS=http://localhost:5173
