# Secrets
.env
.env.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node
node_modules/
dist/

# Runtime data (uploads, reviews, progress records)
local-data/

# Accuracy harness output. The labelled designs in fixtures/ground_truth/ are
# committed; the reports generated from them are run output, not fixtures.
fixtures/ground_truth/accuracy-report-*.md
fixtures/ground_truth/accuracy-report-*.json
