fastapi==0.121.2
uvicorn[standard]==0.38.0
pydantic==2.12.4
anthropic==0.75.0        # LLM_PROVIDER=anthropic path (kept for same-day revert)
openai==2.49.0           # LLM_PROVIDER=openrouter path, OpenAI-compatible client
httpx==0.28.1            # llm.py imports it directly for the shared transport it
                         # hands the SDK, so pin it here rather than inheriting
                         # whatever anthropic/openai happen to resolve it to
jsonschema==4.26.0       # response validation — the real enforcement layer
python-multipart==0.0.20
python-dotenv==1.2.1
pypdf==6.1.1
python-docx==1.2.0
reportlab==5.0.0     # PDF export; pulls pillow in for diagram embedding
# Pinned DIRECTLY rather than left to reportlab's resolver. `ingestion/embedded.py`
# decodes embedded PDF images with it, so it is now a dependency this code has of
# its own — and a transitive pin is one `pip install -U reportlab` away from moving
# or disappearing under a module that needs it.
pillow==12.3.0
